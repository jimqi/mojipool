import React, { Component } from 'react';
import { connect } from 'react-redux';

class TripList extends Component {
    render() {
        return (
            <div> TripList Placeholder </div>
        )
    }
}

export default connect()(TripList);