import React, { Component } from 'react';
import { connect } from 'react-redux';

class InfoPanel extends Component {
    render() {
        return (
            <div> Infopanel Placeholder </div>
        )
    }
}

export default connect()(InfoPanel);